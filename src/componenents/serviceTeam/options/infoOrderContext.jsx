import React, { useContext } from "react"
import { StateOrderContext } from "./orderContext";

const InfoContext = props => {

    const { state } = useContext(StateContext)

    return (
    
    )
}

export default InfoContext;